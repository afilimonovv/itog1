pipeline {
    agent any
    tool name: 'terraform101', type: 'terraform'
    stages {
        stage ('terraforminit') {
            steps {
                sh 'terraform init'
                sh 'terraform --version'
            }
        }

    }
}